- clean: ["~"]

- shell:
    - command: ./install -c install.conf.core.yaml
      description: Install core dotfiles
      stdin: true
      stdout: true
    - command: ./install -c install.conf.managers.yaml
      description: Install mangers dotfiles
      stdin: true
      stdout: true
    - command: ./install -c install.conf.npm.yaml
      description: Install npm dotfiles
      stdin: true
      stdout: true
    - command: ./install -c install.conf.vscode.yaml
      description: Install vscode dotfiles
      stdin: true
      stdout: true
